# RBAC

Business roles and platform administration are separate scopes. A business membership may have multiple roles and explicit outlet assignments; absence of an outlet assignment does not mean all-outlet access. Owner last-member protection is transactional.

Permissions are checked in the API on every sensitive read and mutation. UI navigation only reflects permissions; it never substitutes for authorization.
